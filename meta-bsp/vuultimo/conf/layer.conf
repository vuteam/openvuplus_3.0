BBFILES += "${LAYERDIR}/recipes/*/*.bb"
BBFILES += "${LAYERDIR}/recipes/*/*.bbappend"
BBPATH .= ":${LAYERDIR}"
BBFILE_COLLECTIONS += "vuultimo"
BBFILE_PRIORITY_vuultimo = "40"
BBFILE_PATTERN_vuultimo := "^${LAYERDIR}/"
